[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Handle SPA routing by redirecting everything to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200